<%= form_for(key_donor) do |f| %>
  <% if key_donor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(key_donor.errors.count, "error") %> prohibited this key_donor from being saved:</h2>

      <ul>
      <% key_donor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  

 

  
<% end %>
